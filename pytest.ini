[pytest]
testpaths = tests
markers =
    agentflow: current AgentFlow control-plane and focused regression tests
    legacy: pre-AgentFlow Trace/Grid/Chief/CONCH and auxiliary legacy tests
    architecture_baseline: Frozen CONCH 5x architecture baseline tests
    gpu: tests requiring a visible CUDA GPU (GPU 0 in the formal baseline)
