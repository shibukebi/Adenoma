# Artifacts

This directory stores:

- `runs/`: case-level outputs, logs, and summaries
- `cache/`: reusable trace and observation caches

Large intermediate data still comes from the existing `adenoma` project and its configured output paths.
